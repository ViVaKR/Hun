[package]
name = "hun_macro"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true # 🔑 Rust 속성 매크로 전용 라이브러리로 지정!

[dependencies]
syn = { version = "3.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"