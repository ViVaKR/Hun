# LSL, LSR, ASR

>- `.quad` 고정 레코드 테이블 : 컴파일 타임에 고정 (8바이트), (끝 - 시작) >> 시프트값 을 계산
>>- 컴파일 타임에 크기가 고정된 레코드
>- `.asciz` 문자열 : 실행전엔 모름 (가변), ldrb + 널 체크 루프로 직접 세기 (strlen 방식)

### LSL (Logical Shift Left)

### LSR (Logical Shift Right)

### ASR (Arithmetic Shift Right)